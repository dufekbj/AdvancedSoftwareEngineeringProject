# Core Python utilities
numpy
matplotlib

# Mutation testing tool
# MutPy is stable on Python 3.10 (3.11+ has issues); pin your Python to 3.10.x in CI/venv.
mutpy==0.6.1  # or whichever version is stable for your environment
PyYAML>=6.0.0

# For running subprocess + test harnesses nicely (optional)
pytest
pytest-timeout

# For data serialization, experiment tracking
pandas

# (Optional) If you want pretty plots or notebooks
jupyter
seaborn

# (Optional) CLI quality of life
tqdm

# (Optional) Static type checking
mypy
